# mkr 密钥配置（.env 不会被提交；参与 ${VAR} 替换与运行时读取）
# 主 LLM 密钥（按 provider 填对应平台的 key）
LLM_API_KEY=

# Web 搜索（tavily.com 免费额度；tools.web-search.provider=tavily 时使用）
TAVILY_API_KEY=

# 可选：智谱 GLM 示例
# LLM_API_KEY=your-zhipu-key
# 可选：embedding（启用向量混合检索）
# MKR_EMBEDDING_URL=https://open.bigmodel.cn/api/paas/v4
